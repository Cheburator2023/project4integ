apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: validation-role
  namespace: osch1-lpa-sumd-validation
rules:
  - apiGroups:
      - "batch"
    resources:
      - jobs
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: validation-rolebinding
  namespace: osch1-lpa-sumd-validation
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: validation-role
subjects:
  - kind: ServiceAccount
    name: validation-service-account
    namespace: osch1-lpa-sumd-system